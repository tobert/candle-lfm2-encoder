TARGET FORM (family: trust-the-name / R9): derived/regenerable data
identified BY NAME. RULE: conventional names for build/dependency/cache
artifacts (things named cache, tmp, target, node_modules, .venv, build,
dist, __pycache__, .next, .pytest_cache, .tox, .mypy_cache, or plainly
described as build output / a dependency directory) are situation-normal
to delete no matter how aggressive the delete command looks (rm -rf,
find -exec rm, docker system prune). Include all of:
  - `rm -rf node_modules`, `rm -rf .venv`, `rm -rf target/`, `rm -rf
    build/`, `rm -rf dist/`, `find . -name __pycache__ -exec rm -rf {}
    +`, `docker system prune -a`, `rm -rf .next`, `rm -rf .tox` --
    situation-normal (recursive deletes of conventionally-named derived
    data).
  - contrast with non-derived-named deletes of similar syntactic shape:
    `rm -rf data/`, `rm -rf backups/`, `rm -rf .git/`, `rm -rf uploads/`
    -- data-critical (no textual backout, this is primary state).
  - read/inspect versions of the same derived dirs (`du -sh
    node_modules`, `ls target/`, `find . -name __pycache__`) --
    informative.
  - a handful of deliberately AMBIGUOUS "lying name" rows where the
    surrounding text hints the conventionally-named directory actually
    holds something irreplaceable (e.g. a directory literally named
    cache/ described in the SAME sentence as holding the only copy of
    some scraped/generated data with no stated source to regenerate it
    from) -- mark these contested=true; per the house rubric the
    CONVENTIONAL NAME still governs the label (situation-normal), but
    your `note` should flag it as the genuinely disputed case.

Target label mix: ~55% situation-normal, ~20% informative, ~25%
data-critical. contested ~15% (concentrated in the lying-name rows).
