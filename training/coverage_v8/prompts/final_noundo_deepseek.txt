You are generating synthetic training data for a text classifier that scores
shell statements and occasional natural-language operational requests on a
DATA RECOVERABILITY axis. Write entirely ORIGINAL, generic dev-ops /
sysadmin / software-engineering shell text -- invented package names,
paths, hostnames (use example.com-style or clearly fictitious names only,
never real companies/infra), not copied from any dataset, tutorial, or
real system. Output EXACTLY 70 JSON lines, one object per line, and NOTHING
ELSE -- no code fences, no markdown, no numbering, no commentary, no blank
lines, no trailing text after the last line.

Each line must be exactly this JSON object shape:
{"text": "<the statement>", "label": "informative"|"situation-normal"|"data-critical", "verb": "<primary verb/command, lowercase>", "resource": "<primary target/object, lowercase>", "contested": true|false, "note": "<one-line reasoning>", "author": "deepseek-noundo"}

LABEL RUBRIC (three labels, ordinal: informative < situation-normal < data-critical):
- informative: observes only -- reads, listings, dry-runs, pure streaming
  that leaves no durable artifact, ephemeral process-local state (cd,
  pushd/popd, setting a shell variable).
- situation-normal: changes durable state with an evident backout -- a
  created artifact can simply be deleted (installed dependency tree,
  cloned repo, downloaded file, built image, ordinary git push to a
  feature branch); routine reversible operations (restarts, scaling,
  config apply).
- data-critical: destroys or irreversibly changes data with NO backout
  shown in the text, OR is an action with no undo anywhere at all
  (publishing, sending, posting) even when nothing pre-existing is
  destroyed.

`text` should read like realistic terminal input: a single command, a
&&/;/| chain, or occasionally a short natural-language ops request --
NOT a full script or YAML document. Length 3-40 words.
contested=true only for genuinely arguable rows (target ~10-15% of the
batch unless told otherwise below).

TARGET FORM (family: no-undo-anywhere / R7): actions with NO UNDO
ANYWHERE. RULE: irreversibility alone makes a statement data-critical
even when nothing pre-existing is destroyed. Include all of:
  - package/registry publishes: `npm publish`, `npm publish --tag
    latest`, `cargo publish`, `twine upload dist/*`, `docker push
    registry.example.com/app:1.4.0`, `gh release create v2.0.0
    dist/*.tar.gz` -- data-critical.
  - contrast with their dry-run/local-only counterparts: `npm publish
    --dry-run`, `cargo publish --dry-run`, `docker build -t app:1.4.0 .`
    (build only, not pushed) -- informative or situation-normal
    respectively.
  - one-way communications: sending an email (`mail -s "..."
    user@example.com < body.txt`, curling a transactional-email API),
    posting to a chat webhook (`curl -X POST
    https://hooks.example.com/services/xxx -d '{"text":"..."}'`),
    posting a public code-review or issue comment via API, filing a
    support ticket via API -- data-critical regardless of message
    content or severity.
  - contrast: reading/listing prior sent messages, or drafting without
    sending (`echo "draft: ..." > draft.txt`) -- informative.
  - git history actions with no undo: `git push --force origin main`,
    `git push --force-with-lease origin release/2.0` to a SHARED branch,
    rewriting and pushing tags (`git tag -f v1.0 && git push origin
    --tags`) -- data-critical (history rewrite / no-undo tag
    semantics). Contrast with an ordinary `git push origin
    feature/my-branch` (situation-normal -- normal branch push,
    revertible) and a force-push explicitly to a clearly-labeled
    private/local/solo scratch branch (situation-normal, may be
    contested).
Build several minimal pairs (publish vs --dry-run publish; push vs push
--force; draft vs sent) so the label hinges on the one word or flag that
matters.

Target label mix: ~55% data-critical, ~25% informative, ~20%
situation-normal. contested ~12%.

DIVERSITY: every row structurally distinct, no template stamping; vary
tool/command choice and invented path/package/db/branch names widely.
Every row must have "author": "deepseek-noundo" exactly.

Validate before finishing: every line you emit must parse as JSON with
exactly the 7 keys text/label/verb/resource/contested/note/author, and a
valid label. Write EXACTLY 70 rows. Output ONLY the JSON lines.
