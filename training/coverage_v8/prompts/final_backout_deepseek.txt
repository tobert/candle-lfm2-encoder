You are generating synthetic training data for a text classifier that scores
shell statements and occasional natural-language operational requests on a
DATA RECOVERABILITY axis. Write entirely ORIGINAL, generic dev-ops /
sysadmin / software-engineering shell text -- invented package names,
paths, hostnames (use example.com-style or clearly fictitious names only,
never real companies/infra), not copied from any dataset, tutorial, or
real system. Output EXACTLY 90 JSON lines, one object per line, and NOTHING
ELSE -- no code fences, no markdown, no numbering, no commentary, no blank
lines, no trailing text after the last line.

Each line must be exactly this JSON object shape:
{"text": "<the statement>", "label": "informative"|"situation-normal"|"data-critical", "verb": "<primary verb/command, lowercase>", "resource": "<primary target/object, lowercase>", "contested": true|false, "note": "<one-line reasoning>", "author": "deepseek-backout"}

LABEL RUBRIC (three labels, ordinal: informative < situation-normal < data-critical):
- informative: observes only -- reads, listings, dry-runs, pure streaming
  that leaves no durable artifact, ephemeral process-local state (cd,
  pushd/popd, setting a shell variable).
- situation-normal: changes durable state with an evident backout -- a
  created artifact can simply be deleted (installed dependency tree,
  cloned repo, downloaded file, built image, ordinary git push to a
  feature branch); routine reversible operations (restarts, scaling,
  config apply).
- data-critical: destroys or irreversibly changes data with NO backout
  shown in the text, OR is an action with no undo anywhere at all
  (publishing, sending, posting) even when nothing pre-existing is
  destroyed.

`text` should read like realistic terminal input: a single command, a
&&/;/| chain, or occasionally a short natural-language ops request --
NOT a full script or YAML document. Length 3-40 words.
contested=true only for genuinely arguable rows (target ~10-15% of the
batch unless told otherwise below).

TARGET FORM (family: backout chains / Rule 7): backup-then-mutate /
snapshot-then-delete CHAINS versus the same destructive tail alone. RULE:
a statement is judged as a WHOLE -- if a backup/snapshot/export of the
SAME data appears earlier in the same statement, it IS the backout and
the chain is situation-normal; if no part of the statement backs out the
destructive part, the chain takes its most severe part's label. Include
all of:
  - real backout chains (situation-normal): `pg_dump mydb >
    mydb_backup.sql && dropdb mydb`, `cp -r data data.bak && rm -rf
    data`, `tar czf logs_archive.tar.gz logs/ && rm -rf logs/`,
    `redis-cli SAVE && redis-cli FLUSHALL`, `mysqldump prod >
    prod_2026-08-10.sql && mysql -e 'DROP TABLE prod.sessions'`.
  - the SAME destructive tail with NO preceding backup (data-critical):
    `dropdb mydb`, `rm -rf data`, `redis-cli FLUSHALL`, `mysql -e 'DROP
    TABLE prod.sessions'`.
  - FAKE/MISMATCHED backouts -- a backup of something OTHER than what's
    destroyed (still data-critical: tests literal-coverage reading, not
    just the presence of the word "backup"): `cp config.yaml
    config.yaml.bak && rm -rf /var/lib/postgres/data`, `tar czf
    old_logs.tar.gz old_logs/ && redis-cli FLUSHALL`.
  - snapshot/backup-adjacent reads (informative): `ls *.sql.bak`,
    `pg_dump --schema-only mydb` (schema-only dry inspection, no
    destructive tail), `redis-cli --scan --pattern 'backup:*'`.
Build several EXACT TRIPLES (real chain / bare tail / mismatched-backup
chain) around the same underlying destructive action so the label is
forced by statement structure, not by vocabulary like the word "backup"
appearing anywhere.

Target label mix: ~45% situation-normal, ~40% data-critical, ~15%
informative. contested ~10%.

DIVERSITY: every row structurally distinct, no template stamping; vary
tool/command choice and invented path/package/db/branch names widely.
Every row must have "author": "deepseek-backout" exactly.

Validate before finishing: every line you emit must parse as JSON with
exactly the 7 keys text/label/verb/resource/contested/note/author, and a
valid label. Write EXACTLY 90 rows. Output ONLY the JSON lines.
