You are generating synthetic training data for a text classifier that scores
shell statements and occasional natural-language operational requests on a
DATA RECOVERABILITY axis. Write entirely ORIGINAL, generic dev-ops /
sysadmin / software-engineering shell text -- invented package names,
paths, hostnames (use example.com-style or clearly fictitious names only,
never real companies/infra), not copied from any dataset, tutorial, or
real system. Output EXACTLY 30 JSON lines, one object per line, and NOTHING
ELSE -- no code fences, no markdown, no numbering, no commentary, no blank
lines, no trailing text after the last line.

Each line must be exactly this JSON object shape:
{"text": "<the statement>", "label": "informative"|"situation-normal"|"data-critical", "verb": "<primary verb/command, lowercase>", "resource": "<primary target/object, lowercase>", "contested": true|false, "note": "<one-line reasoning>", "author": "orglm-trustname"}

LABEL RUBRIC (three labels, ordinal: informative < situation-normal < data-critical):
- informative: observes only -- reads, listings, dry-runs, pure streaming
  that leaves no durable artifact, ephemeral process-local state (cd,
  pushd/popd, setting a shell variable).
- situation-normal: changes durable state with an evident backout -- a
  created artifact can simply be deleted (installed dependency tree,
  cloned repo, downloaded file, built image, ordinary git push to a
  feature branch); routine reversible operations (restarts, scaling,
  config apply).
- data-critical: destroys or irreversibly changes data with NO backout
  shown in the text, OR is an action with no undo anywhere at all
  (publishing, sending, posting) even when nothing pre-existing is
  destroyed.

`text` should read like realistic terminal input: a single command, a
&&/;/| chain, or occasionally a short natural-language ops request --
NOT a full script or YAML document. Length 3-40 words.
contested=true only for genuinely arguable rows (target ~10-15% of the
batch unless told otherwise below).

TARGET FORM (family: trust-the-name / R9): derived/regenerable data
identified BY NAME. RULE: conventional names for build/dependency/cache
artifacts (things named cache, tmp, target, node_modules, .venv, build,
dist, __pycache__, .next, .pytest_cache, .tox, .mypy_cache, or plainly
described as build output / a dependency directory) are situation-normal
to delete no matter how aggressive the delete command looks (rm -rf,
find -exec rm, docker system prune). Include all of:
  - `rm -rf node_modules`, `rm -rf .venv`, `rm -rf target/`, `rm -rf
    build/`, `rm -rf dist/`, `find . -name __pycache__ -exec rm -rf {}
    +`, `docker system prune -a`, `rm -rf .next`, `rm -rf .tox` --
    situation-normal (recursive deletes of conventionally-named derived
    data).
  - contrast with non-derived-named deletes of similar syntactic shape:
    `rm -rf data/`, `rm -rf backups/`, `rm -rf .git/`, `rm -rf uploads/`
    -- data-critical (no textual backout, this is primary state).
  - read/inspect versions of the same derived dirs (`du -sh
    node_modules`, `ls target/`, `find . -name __pycache__`) --
    informative.
  - a handful of deliberately AMBIGUOUS "lying name" rows where the
    surrounding text hints the conventionally-named directory actually
    holds something irreplaceable (e.g. a directory literally named
    cache/ described in the SAME sentence as holding the only copy of
    some scraped/generated data with no stated source to regenerate it
    from) -- mark these contested=true; per the house rubric the
    CONVENTIONAL NAME still governs the label (situation-normal), but
    your `note` should flag it as the genuinely disputed case.

Target label mix: ~55% situation-normal, ~20% informative, ~25%
data-critical. contested ~15% (concentrated in the lying-name rows).

DIVERSITY: every row structurally distinct, no template stamping; vary
tool/command choice and invented path/package/db/branch names widely.
Every row must have "author": "orglm-trustname" exactly.

Validate before finishing: every line you emit must parse as JSON with
exactly the 7 keys text/label/verb/resource/contested/note/author, and a
valid label. Write EXACTLY 30 rows. Output ONLY the JSON lines.
